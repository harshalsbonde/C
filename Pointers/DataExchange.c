//////////////////////////////////////////////////////////////////////////////
#include <stdio.h> 
#include <stdlib.h> 
/////////////////////////////////////////////////////////////////////////////
void alloc_integer(int** pp_int, int init_n); 
/////////////////////////////////////////////////////////////////////////////
int main(void)
{
	int* p_int = NULL; 
	alloc_integer(&p_int, 500); 
	printf("Value of dynamically allocated integer:%d\n", *p_int); 
	free(p_int); 
	p_int = NULL; 
	return (0); 
}
///////////////////////////////////////////////////////////////////////////////
void alloc_integer(int** pp_int, int init_n)
{
	int* ptr = NULL; 
	ptr = (int*)malloc(sizeof(int)); 
	if (ptr == NULL)
	{
		puts("Error in allocating memory"); 
		exit(EXIT_FAILURE); 
	}
	*ptr = init_n; 
	*pp_int = ptr; 
}
///////////////////////////////////////////////////////////////////////////////
